# 🚀 GitHub Deployer

<div align="center">

![Python](https://img.shields.io/badge/Python-3.8+-blue?style=for-the-badge&logo=python&logoColor=white)
![GitHub](https://img.shields.io/badge/GitHub-API-black?style=for-the-badge&logo=github&logoColor=white)
![License](https://img.shields.io/badge/License-Proprietary-red?style=for-the-badge)

**Автоматический деплой проекта на GitHub с проверкой всех путей и файлов**

</div>

---

## 📋 Описание

Этот скрипт автоматизирует процесс деплоя вашего проекта на GitHub:

- ✅ Проверяет наличие всех необходимых файлов и папок
- ✅ Валидирует пути перед деплоем
- ✅ Создает репозиторий на GitHub (если не существует)
- ✅ Инициализирует Git и делает коммит
- ✅ Пушит код на GitHub

---

## ⚡ Быстрый старт

### 1. Установите переменные окружения

**Windows (CMD):**
```cmd
set GITHUB_TOKEN=ghp_ваш_токен_здесь
set GITHUB_USERNAME=ваш_username
set REPO_NAME=pool-landing
```

**Windows (PowerShell):**
```powershell
$env:GITHUB_TOKEN = "ghp_ваш_токен_здесь"
$env:GITHUB_USERNAME = "ваш_username"
$env:REPO_NAME = "pool-landing"
```

**Linux/macOS:**
```bash
export GITHUB_TOKEN=ghp_ваш_токен_здесь
export GITHUB_USERNAME=ваш_username
export REPO_NAME=pool-landing
```

### 2. Запустите скрипт

```bash
python deploy/deploy_to_github.py
```

---

## 🔑 Получение GitHub Token

1. Перейдите в [GitHub Settings → Developer settings → Personal access tokens](https://github.com/settings/tokens)
2. Нажмите **"Generate new token (classic)"**
3. Выберите права:
   - ✅ `repo` (полный доступ к репозиториям)
   - ✅ `workflow` (если используете GitHub Actions)
4. Скопируйте токен (он показывается только один раз!)

---

## 📁 Структура деплоя

Скрипт автоматически деплоит следующие файлы:

```
📦 Проект
├── 📄 App.tsx
├── 📄 index.tsx
├── 📄 index.html
├── 📄 package.json
├── 📄 tsconfig.json
├── 📄 vite.config.ts
├── 📁 components/
│   ├── Header.tsx
│   ├── Hero.tsx
│   ├── Footer.tsx
│   └── ...
└── 📁 .vscode/
```

**Исключаются:**
- `node_modules/`
- `.env.local`
- `deploy/`
- `__pycache__/`

---

## 🛡️ Безопасность

> ⚠️ **ВАЖНО:** Никогда не коммитьте токены в репозиторий!

- Токен передается через переменные окружения
- Файл `.env.local` автоматически исключается из деплоя
- Используйте токены с минимально необходимыми правами

---

## 📜 Лицензия

```
Copyright (c) 2025. All Rights Reserved.

Этот код является собственностью автора.
Несанкционированное копирование, модификация или распространение
строго запрещены без письменного разрешения правообладателя.
```

---

## 🤝 Поддержка

Если возникли проблемы:

1. Убедитесь, что Git установлен: `git --version`
2. Проверьте правильность токена
3. Убедитесь, что username указан верно

---

<div align="center">

**Made with ❤️ for developers**

</div>
